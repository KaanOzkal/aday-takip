<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Yönetici Girişi | Berliner Akademie</title>
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;600;700;800&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <style>
        body {
            background: radial-gradient(circle at center, #0f172a 0%, #020617 100%);
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            font-family: 'Plus Jakarta Sans', sans-serif;
            color: white;
        }
        .login-card {
            background: rgba(255, 255, 255, 0.03);
            backdrop-filter: blur(20px);
            border: 1px solid rgba(255, 255, 255, 0.1);
            padding: 3rem;
            border-radius: 24px;
            width: 100%;
            max-width: 420px;
            box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.5);
            text-align: center;
        }
        .form-control {
            background: rgba(0, 0, 0, 0.3);
            border: 1px solid rgba(255, 255, 255, 0.1);
            color: white;
            padding: 14px;
            font-size: 0.95rem;
            border-radius: 12px;
        }
        .form-control:focus {
            background: rgba(0, 0, 0, 0.5);
            border-color: #3b82f6;
            color: white;
            box-shadow: 0 0 0 4px rgba(59, 130, 246, 0.1);
        }
        .form-control::placeholder { color: rgba(255,255,255,0.3); }
        
        .btn-login {
            background: linear-gradient(135deg, #3b82f6, #2563eb);
            border: none;
            padding: 14px;
            font-weight: 700;
            letter-spacing: 0.5px;
            border-radius: 12px;
            margin-top: 10px;
            transition: all 0.3s;
        }
        .btn-login:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 20px -5px rgba(37, 99, 235, 0.4);
        }
        .logo-area {
            width: 80px; height: 80px;
            background: rgba(255,255,255,0.1);
            border-radius: 50%;
            display: flex; align-items: center; justify-content: center;
            margin: 0 auto 1.5rem auto;
            border: 1px solid rgba(255,255,255,0.1);
        }
    </style>
</head>
<body>

    <div class="login-card">
        <div class="logo-area">
            <i class="fas fa-user-shield fa-2x text-primary"></i>
        </div>
        <h3 class="fw-bold mb-1">Yönetici Paneli</h3>
        <p class="text-white-50 mb-4 small">Lütfen yetkili hesap bilgilerinizi giriniz.</p>

        <% if (typeof error !== 'undefined') { %>
            <div class="alert alert-danger py-2 small border-0 bg-danger bg-opacity-25 text-danger mb-4">
                <i class="fas fa-exclamation-circle me-1"></i> <%= error %>
            </div>
        <% } %>

        <form action="/admin/login" method="POST">
            <div class="mb-3 text-start">
                <label class="small text-white-50 ms-1 mb-1">Kullanıcı Adı</label>
                <div class="input-group">
                    <span class="input-group-text bg-transparent border-secondary border-opacity-25 text-secondary"><i class="fas fa-user"></i></span>
                    <input type="text" name="username" class="form-control" placeholder="admin" required autocomplete="off">
                </div>
            </div>
            
            <div class="mb-4 text-start">
                <label class="small text-white-50 ms-1 mb-1">Şifre</label>
                <div class="input-group">
                    <span class="input-group-text bg-transparent border-secondary border-opacity-25 text-secondary"><i class="fas fa-lock"></i></span>
                    <input type="password" name="password" class="form-control" placeholder="••••••••" required>
                </div>
            </div>

            <button type="submit" class="btn btn-primary w-100 btn-login text-white">
                GÜVENLİ GİRİŞ YAP
            </button>
        </form>
        
        <div class="mt-4 pt-4 border-top border-white border-opacity-10">
            <a href="/login" class="text-decoration-none text-white-50 small">
                <i class="fas fa-arrow-left me-1"></i> Aday Girişine Dön
            </a>
        </div>
    </div>

</body>
</html>