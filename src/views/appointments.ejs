<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Randevularım</title>
    
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <style>
        body { background-color: #f8fafc; font-family: 'Plus Jakarta Sans', sans-serif; }
        
        .app-card {
            background: white; border-radius: 16px; padding: 20px;
            box-shadow: 0 4px 6px -1px rgba(0,0,0,0.05);
            border: 1px solid #e2e8f0;
            border-left: 5px solid transparent; /* Sol çizgi için yer */
            transition: transform 0.2s;
        }
        .app-card:hover { transform: translateY(-3px); }

        /* Durumlara Göre Renkler */
        .status-wait { border-left-color: #f59e0b; } /* Sarı - Beklemede */
        .status-success { border-left-color: #10b981; } /* Yeşil - Onaylandı */
        .status-danger { border-left-color: #ef4444; } /* Kırmızı - Reddedildi */

        .badge-wait { background: #fffbeb; color: #d97706; border: 1px solid #fcd34d; }
        .badge-success { background: #ecfdf5; color: #059669; border: 1px solid #6ee7b7; }
        .badge-danger { background: #fef2f2; color: #dc2626; border: 1px solid #fca5a5; }

        .date-box {
            background: #f1f5f9; border-radius: 12px; padding: 10px 15px;
            text-align: center; font-weight: bold; color: #334155;
        }
    </style>
</head>
<body>

    <nav class="navbar navbar-light bg-white border-bottom mb-4 py-3">
        <div class="container">
            <div class="d-flex align-items-center gap-3">
                <a href="/panel" class="btn btn-light rounded-circle border"><i class="fas fa-arrow-left"></i></a>
                <h5 class="fw-bold m-0 text-dark">Randevularım</h5>
            </div>
            <button class="btn btn-primary rounded-pill px-4 fw-semibold" data-bs-toggle="modal" data-bs-target="#newAppModal">
                <i class="fas fa-plus me-2"></i>Yeni Randevu
            </button>
        </div>
    </nav>

    <div class="container pb-5">
        <div class="row g-4">
            
            <% if (appointments && appointments.length > 0) { %>
                <% appointments.forEach(app => { %>
                    <% 
                        let borderClass = 'status-wait';
                        let badgeClass = 'badge-wait';
                        let icon = 'fa-hourglass-half';
                        let text = 'İnceleniyor';

                        if (app.status === 'Onaylandı') {
                            borderClass = 'status-success';
                            badgeClass = 'badge-success';
                            icon = 'fa-check-circle';
                            text = 'Onaylandı';
                        } else if (app.status === 'Reddedildi') {
                            borderClass = 'status-danger';
                            badgeClass = 'badge-danger';
                            icon = 'fa-times-circle';
                            text = 'Reddedildi';
                        }
                    %>

                    <div class="col-md-6 col-lg-4">
                        <div class="app-card <%= borderClass %>">
                            <div class="d-flex justify-content-between align-items-center mb-3">
                                <span class="badge <%= badgeClass %> px-3 py-2 rounded-pill fw-bold">
                                    <i class="fas <%= icon %> me-1"></i> <%= text %>
                                </span>
                                <small class="text-muted"><%= new Date(app.createdAt).toLocaleDateString('tr-TR') %></small>
                            </div>

                            <h5 class="fw-bold text-dark mb-1"><%= app.type %></h5>
                            
                            <div class="d-flex align-items-center gap-3 mt-3">
                                <div class="date-box">
                                    <div class="fs-4"><%= app.time %></div>
                                    <small class="text-secondary fw-normal">
                                        <%= new Date(app.date).toLocaleDateString('tr-TR', { day: 'numeric', month: 'short' }) %>
                                    </small>
                                </div>
                                <div class="text-secondary small">
                                    <% if(app.status === 'Onaylandı') { %>
                                        <div class="text-success fw-bold"><i class="fas fa-check me-1"></i>Kesinleşti</div>
                                        <div>Lütfen zamanında hazır olun.</div>
                                    <% } else if(app.status === 'Reddedildi') { %>
                                        <div class="text-danger fw-bold"><i class="fas fa-ban me-1"></i>İptal Edildi</div>
                                        <div>Yeni bir tarih seçebilirsiniz.</div>
                                    <% } else { %>
                                        <div>Yönetici onayı bekleniyor.</div>
                                    <% } %>
                                </div>
                            </div>
                        </div>
                    </div>
                <% }) %>
            <% } else { %>
                <div class="col-12 text-center py-5">
                    <div class="text-muted opacity-25 mb-3"><i class="fas fa-calendar-times fa-4x"></i></div>
                    <h5 class="fw-bold">Henüz Randevunuz Yok</h5>
                    <p class="text-muted">Yeni bir kariyer adımı için randevu oluşturun.</p>
                </div>
            <% } %>

        </div>
    </div>

    <div class="modal fade" id="newAppModal" tabindex="-1">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title fw-bold">Randevu Talep Et</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                </div>
                <form action="/appointments/create" method="POST">
                    <div class="modal-body">
                        <div class="mb-3">
                            <label class="fw-bold mb-1">Konu</label>
                            <select name="type" class="form-select" required>
                                <option value="Vize Görüşmesi">Vize Görüşmesi</option>
                                <option value="Evrak Teslimi">Evrak Teslimi</option>
                                <option value="Danışmanlık">Danışmanlık</option>
                                <option value="Dil Sınavı">Dil Sınavı</option>
                            </select>
                        </div>
                        <div class="row">
                            <div class="col-6 mb-3">
                                <label class="fw-bold mb-1">Tarih</label>
                                <input type="date" name="date" class="form-control" required>
                            </div>
                            <div class="col-6 mb-3">
                                <label class="fw-bold mb-1">Saat</label>
                                <input type="time" name="time" class="form-control" required>
                            </div>
                        </div>
                    </div>
                    <div class="modal-footer">
                        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Vazgeç</button>
                        <button type="submit" class="btn btn-primary">Talep Oluştur</button>
                    </div>
                </form>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>